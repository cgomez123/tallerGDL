# La llave de API de Anthropic de TU agente (no la compartas con nadie).
# Se crea en https://console.anthropic.com > API Keys.
# En Vercel se configura como variable de entorno, nunca se sube al repo.
ANTHROPIC_API_KEY=
